node_modules/
.env
.env.local
.env.production
data-test/
dashboard.html

# Runtime data — personal food logs, OAuth tokens, custom foods.
# Persisted locally + backed up to Telegram (backup.json). Never tracked in git,
# otherwise `git pull` overwrites/erases live data.
data/*.json

.expo/
